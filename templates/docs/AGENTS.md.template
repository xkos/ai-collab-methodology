# {{PROJECT_NAME}} AGENTS.md 模板

> 此文件由 bootstrap prompt 引导 AI 根据项目信息生成。
> 以下为骨架结构，具体内容需要根据项目填充。

# {{PROJECT_NAME}}

（一句话项目描述）

## 项目结构

- `docs/prds/` — 产品需求文档（me2ai：人维护）
- `docs/tech/` — 技术文档（me2ai：人维护）
- `docs/ai2ai/` — 项目状态与迭代记录（ai2ai：AI 维护，人审核）
（补充项目特有的目录）

## 架构概览

（项目的架构简述）

## 全局规范

（项目的编码规范、依赖管理规则等）

## 迭代工作流（必须遵守）

每次开始实现功能前，必须按以下流程执行：

### 0. 需求澄清（按需）+ 确认 Git 状态
- 收到需求后，如果描述模糊、开放或有多种可行方案，**先提问澄清，不直接写代码**
- 明确"做什么"和"不做什么"的边界（YAGNI — 不提前预设未来需求）
- 复杂需求分段确认，输出设计摘要等用户确认后再建 task
- 如果需求已明确（详细步骤、明确 bug、已有 task），可跳过澄清
- `git branch -a` + `git status` + `git log --oneline -5` — 确认当前分支、工作区状态
- 从 main 创建迭代分支 `iter/NNN-xxx`

### 1. 读取当前状态
- 读 `docs/ai2ai/status.md` 了解项目当前阶段和模块就绪度
- 读 `docs/ai2ai/decisions.md` 了解已有的踩坑记录，避免重复踩坑

### 2. 创建 task 文件
- 创建 `docs/ai2ai/tasks/NNN-xxx.md`，定义迭代目标和任务分解
- 每个任务有明确的验证标准
- 单次迭代任务数 3-8 个，超过则拆分

### 3. 实现（TDD + 自审）
- 逻辑层先行：model → store → service → api
- UI 层跟进：基于已就绪的 API 实现界面
- **TDD 红-绿-重构**（service / store / infra 层）：先写失败测试 → 最小实现让测试通过 → 重构 → 提交。不允许先写实现再补测试
- **AI 自审**：每完成一个 task，在标记完成前检查——规格合规、代码质量、测试覆盖、回归影响，发现问题立即修复
- 每完成一个任务，运行测试确认不破坏已有功能，自审通过后再更新 task checkbox
- 单次迭代修改文件超过 10 个时，暂停确认是否需要拆分
- **子代理驱动**（可选）：当有 3 个以上互相独立的任务时，可用子代理并行执行，主代理负责分发和 review

### 4. 迭代结束
- **AI 测试门禁**：全量测试通过 — 不通过则不进入下一步
- 生成/更新 `docs/ai2ai/checklist.md`（本迭代新增测试项 + 继承回归项）→ **等人审核 checklist**
- 人执行测试并勾选 checklist → **等人完成测试**
- 创建 `docs/ai2ai/iterations/NNN-xxx.md`（事后总结，含 checklist 测试结果归档）
- 更新 `docs/ai2ai/status.md`（模块就绪度 + 端到端联通状态）
- 按需追加 `docs/ai2ai/decisions.md`（踩坑记录、微决策）
- task 文件状态改为 ✅ 已关闭
- checklist 中的失败项纳入下一迭代修复任务
- 提交 PR（`gh pr create`），等人 Review + 合并到 main

详细格式见 rules/ 中的 ai2ai-maintenance
Git 工作流见 rules/ 中的 git-workflow

## 文档职责划分

| 类别 | 位置 | 维护者 | 内容 |
|------|------|--------|------|
| me2ai | `docs/prds/`, `docs/tech/`, `AGENTS.md`, rules/ | 人 | 需求、架构、编码规范 — "应该是什么样" |
| ai2ai | `docs/ai2ai/` | AI（人审核） | 项目状态、迭代记录、经验库 — "现在实际是什么样" |

ai2ai 文档结构：
- `docs/ai2ai/status.md` — 全局状态快照（每次迭代后覆盖更新）
- `docs/ai2ai/checklist.md` — 用户验收测试清单（AI 生成/维护，人审核/勾选）
- `docs/ai2ai/test-suite.md` — 全局用例库（从 checklist 沉淀，长期回归）
- `docs/ai2ai/tasks/NNN-xxx.md` — 迭代任务分解（事前计划，过程中更新状态）
- `docs/ai2ai/iterations/NNN-xxx.md` — 迭代归档（事后总结，含测试结果归档）
- `docs/ai2ai/decisions.md` — AI 经验库（微决策 + 踩坑 + Boundary 变更）

tasks/ 和 iterations/ 共享编号，一一配对：task 定义"要做什么"，iteration 记录"做完了什么"。

## AI 决策边界

> 框架说明见 rules/ 中的 ai-boundary-framework

### Always do（直接执行）

- 遵循本文件和 rules/ 中的编码规范
- **需求模糊时主动提问澄清**，不带着猜测直接实现
- 更新 ai2ai 文档
- **TDD 红-绿-重构**：service/store/infra 层先写失败测试，再写最小实现，重构后提交
- **每个 task 完成前执行自审**：规格合规、代码质量、测试覆盖、回归影响
- 运行已有的测试和 lint
- 按 task 文件中已审核的任务执行实现
- **YAGNI**：只实现当前任务要求的功能，不提前为未来需求写代码
（补充项目特有的 Always do）

### Ask first（说明意图，等确认）

- 新增 task 中未提及的外部依赖
- 修改数据库 schema
- 改变已有 API 签名
- 删除或重命名已有文件
- 重构涉及超过 3 个文件的变更
- 偏离当前 task 文件中定义的任务范围
（补充项目特有的 Ask first）

### Never do（禁止）

- 删除或覆盖 me2ai 文档的内容
（补充项目特有的 Never do）
